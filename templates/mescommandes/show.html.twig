{% extends 'base.html.twig' %}

{% block title %}Mescommandes{% endblock %}

{% block body %}
    <h1>Mescommandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mescommande.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ mescommande.quantite }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ mescommande.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mescommandes_index') }}">back to list</a>

    <a href="{{ path('app_mescommandes_edit', {'id': mescommande.id}) }}">edit</a>

    {{ include('mescommandes/_delete_form.html.twig') }}
{% endblock %}
